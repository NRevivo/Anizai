# C_cloud/plans/

Future Domain-C deployment plans (e.g. an `anizai-agent` cloud rebuild) land here
when a specific deployment is actively planned; until then, pending cloud work lives
as backlog rows in `cloud_sprints.md §2`.

**Empty by design today** — Domain C has no open cloud implementation sprints (Phase 9
and Phase 9.5 are closed). A backlog item graduates into a plan file here only when Ron
decides to do a specific deployment.

Closed Domain-A sprint plans land here when a `plans/` file closes.

Empty by design — Domain A has no new-style closed plans yet (the open work is
Phase 7B.5, still in `plans/`). Deep closed-sprint history lives in
`../pipeline_archive.md` and, for old flat-format work, `../../task_plan_archive.md`.

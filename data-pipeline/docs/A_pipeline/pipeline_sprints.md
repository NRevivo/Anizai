# pipeline_sprints.md
> Domain: A — Pipeline
> Type: Sprints
> Last updated: 2026-07-27
> TL;DR: Live status of the pipeline — what's closed (Phase 7D closed 2026-07-27), the open sprints (Phase 7B.5 calibration + its 7B.5-I instrumentation prerequisite), deferred items, and the Domain A Known Gaps (KG-A-*). Open this to see what pipeline work remains.

## Navigation
- §1 — Status Summary — every pipeline phase, status, close date, outcome, plan file
- Phase Context / Rationale — why Phase 7B.5 exists (theoretical → empirical)
- §2 — Open Work — Phase 7B.5 filter calibration (reference → `plans/`)
- §3 — Deferred Items — parked work and the condition to revisit
- §4 — Known Gaps — KG-A-* table

---

## §1 — Status Summary

| Sprint / Phase | Status | Closed | Key outcome | Plan file |
|---|---|---|---|---|
| Phase 0 — Foundation | Closed | 2026-03 | Config, Kafka utils, validators, DB, `init.sql`, docker-compose | — |
| Sprints 1–10 — 9 source vertical slices | Closed | 2026-03 → 04 | All 9 producers + Silver/Gold branches + persistence, triple-gate tested | — |
| Sprint 11 — Pre-Phase 5 checkpoint | Closed | 2026-04-09 | 5 bugfixes; Reddit fully removed; prompts extracted to `prompts/` | — |
| Sprint 12 — Dockerfile.flink | Closed | 2026-04-10 | Custom PyFlink 1.19.1 image; EXACTLY_ONCE unblocked | — |
| Sprint 13 — Cross-cutting enrichment | Closed | 2026-04-10 | Translation, consensus helpers, mapping_dict, reactive trigger consumer | — |
| Sprint 14 — Airflow orchestration | Closed | 2026-04-11 | 7 scheduled DAGs; OpenSky OAuth2 migration | — |
| Sprint 15 — Production readiness | Closed | 2026-04-11 | OpenSky OAuth2 live-validated; orchestration test (scraper later retired in Phase 7C) | — |
| Sprint 16 — Monitoring | Closed | 2026-04-11 | Prometheus + Grafana; structured JSON logging; trace_id propagation | — |
| Sprint 17 — Full E2E live validation | Closed | 2026-04-11 | All 9 sources live Bronze→Gold→vaults; PyFlink side-output rewrite | — |
| Phase 7A — Provider migration | Closed | 2026-05-09 | NewsAPI → newsapi.ai (Event Registry); full article body to vault | — |
| Phase 7B — Filter + semantic rescue | Closed | 2026-05-09 | Two-stage gate; threshold 0.09→0.15; semantic rescue live | — |
| Phase 7C — Scraper retirement | Closed | 2026-05-09 | Scraper deleted; `scrape_attempted` dropped; `newspaper4k` removed | — |
| Phase 7B.5 — Filter-threshold calibration | Open (queued) | — | Empirically validate the 7B thresholds + A1 removals on production vault data | `plans/phase7b5_filter_calibration.md` |
| Phase 7B.5-I — Filter observability & cost instrumentation | Open (active) | — | Reject retention + `rescue_cosine` persistence + per-call LLM cost events/views; one-day cloud collection run produces the 7B.5 dataset + the pipeline-day cost number | `plans/phase7b5i_filter_observability_and_cost.md` |
| Phase 7D — Enrichment gating & social-path reject coverage | **Closed** | **2026-07-27** | Enrichment dedup gate (KG-A-7 global_news), deterministic `signal_id` (KG-A-8), HackerNews reject capture (KG-A-12), instance key (KG-A-13) — **verified live on cloud traffic** (arxiv 18.1→1.0, newsapi 1.74→1.0, HN 288 rejects captured, 1:1 vectors, 0 DLQ regression). Social gate **fired but n=2 (not verified)**. Close pack: `reports/phase7d-verify-20260727/` | `archive_plans/phase7d_enrichment_gating.md` |

> Closed rows have no `plans/` file — their detail lives in `pipeline_archive.md`
> (and, for old flat-format work, `../../task_plan_archive.md`).

---

## Phase Context / Rationale

Phase 7B (closed 2026-05-09) set the two-stage filter's thresholds **theoretically**
— before any production data existed. The 10 A1 keyword removals, the
`DEFAULT_THRESHOLD = 0.15` sniper floor, and the `GOLD_SEMANTIC_RESCUE_THRESHOLD = 0.35`
rescue cutoff were chosen from design reasoning against pre-production (snippet-era)
estimates. Phase 7B.5 exists to **validate those values empirically** against real
post-7A `knowledge_vault` rows, because the full-body article shape shifts the score
distributions and calibrating against pre-7A rows would produce stale thresholds. The
≥200-row entry gate is satisfied (616+ rows available), so the sprint is unblocked.

---

## §2 — Open Work

### Phase 7B.5-I — Filter Observability & Cost Instrumentation (active)

**Status:** Open / active (kicked off 2026-07-02).

Instrumentation prerequisite discovered 2026-06-30: the data 7B.5 needs (rejected
articles, rescue cosine scores) is never persisted, so 7B.5 cannot run against
today's vault alone. This sprint adds flag-gated reject retention, `rescue_cosine`
persistence, and permanent per-call LLM cost tracking, then collects one day of
cloud data. Self-contained plan: **`plans/phase7b5i_filter_observability_and_cost.md`**.

### Phase 7B.5 — Filter-Threshold Calibration (queued)

**Status:** Open / queued — data collection now gated on the 7B.5-I day-run.

**Entry gate:** ≥200 post-7A `knowledge_vault` rows in cloud Postgres — **satisfied**
(616+ rows available).

The full task table (T7B.1 / T7B.2 / T7B.9 + the `docs/phase7_filter_analysis.md`
deliverable), the concrete values under validation, the gate model, and the skills
all live in the self-contained plan: **`plans/phase7b5_filter_calibration.md`**. Kick
off there with `sprint-kickoff` + `filter-analysis`.

---

## §3 — Deferred Items

| Item | Deferred from | Reason | Condition to revisit |
|---|---|---|---|
| Market Divergence Alerts | Sprint 1 / Phase 4E | Only one active market platform after PredictIt removal; cross-platform divergence needs two | A second prediction-market platform is added |
| Polymarket comment ingestion | Phase 9.5 | Gamma `/comments` breaking change; correct `entity_entity_type` enum unknown | New API contract resolved (reverse-engineered or vendor support) |
| Re-embedding pre-Sprint-15 truncated rows | Sprint 15 | Resolved by Phase 7A full-body migration — accepted as the new normal, no longer a deferral | n/a (closed) |

---

## §4 — Known Gaps

| ID | Description | Raised in | Priority | Condition to address |
|---|---|---|---|---|
| KG-A-1 | `mapping_dict.find_similar_and_link()` not wired into the Gold pipeline; cross-source canonical linkage is not automatic | Sprint 13 / 15 | Medium | Live pipeline perf data to choose approach A (on-insert) vs B (batch scan) |
| KG-A-2 | `PolymarketGoldMetricsFunction` misnamed — now dispatches 5 metric sources; cosmetic only | Sprint 17 | Low | Any `gold_job.py` refactor |
| KG-A-3 | Google Trends producer 404 — pytrends hits Google's moved unofficial endpoint, 0 Bronze messages (raise-on-0% mitigates silent success) | Surfaced Phase 9.5 | Medium | Upstream pytrends fix, switch to official Trends API, or retire the source |
| KG-A-4 | Polymarket `/comments` breaking change; comment ingestion feature-flagged off. **Latent risk on re-enable (found 2026-07-23):** `process_social_pulse_message` guards only on an empty comment batch — it carries NO `is_high_signal` guard, unlike every other enriched path. Re-enabling comments as the code stands today would send every comment batch to GPT unfiltered. | Surfaced Phase 9.5 | Medium | Reverse-engineer the new `/comments` contract, or retire the comment path. If the path is ever re-enabled, add the `is_high_signal` guard first. |
| KG-A-5 | OpenSky outbound timeout from GKE (`opensky-network.org` unreachable from main-pool node); local ingestion works | Surfaced Phase 9.5 | Medium | GCP firewall/CIDR investigation; possible cloud-IP block by OpenSky |
| KG-A-6 | Dormant `reddit`/`predictit` enum values remain in PostgreSQL CHECK constraints (`init.sql`, `postgres-configmap.yaml`); no active writer | Phase 7 doc note | Low | A later infra/schema migration phase |
| KG-A-7 | Dedup does not gate Gold enrichment — duplicate articles/stories re-run GPT enrichment + embedding on every re-fetch (cost + RPD impact). Affects **both** enriched paths, not only global_news: in `GlobalNewsGoldFunction` the `kv_archive()` call runs before dispatch and its `None` return (= document_hash already present) is discarded; in `PolymarketGoldSocialFunction` the `exists_by_content_hash()` check runs only to resolve `social_id`, and enrichment proceeds regardless. **Day-run evidence (`dayrun-20260722`) — enrichment calls → rows archived:** newsapi 1,360 → 644; arxiv 910 → 108; hackernews 732 → 127; telegram 327 → 327 (the only clean source). Roughly 65% of the day's $1.3399 enrichment spend was paid on objects that were then discarded. | Phase 7B.5-I (quantified 2026-07-23) | High | Gate the Gold dispatch on the exists-check in both paths — in each case the check already runs one step before the LLM call. **Correction:** the original entry cited the social path as the reference pattern; that was inaccurate — neither path gates enrichment today. **✅ CLOSED on the global_news path 2026-07-27 (Phase 7D), live cloud evidence: arxiv 18.1→1.0 and newsapi 1.74→1.0 gold_enrich per distinct item; C3 wasted-enrichment = 0. The SOCIAL half is NOT closed — only n=2 high-signal HN stories exercised; recorded as "fired, not verified" (see `reports/phase7d-verify-20260727/`).** |
| KG-A-8 | global_news builders use `uuid4` signal_id — Flink re-deliveries and duplicate articles accumulate duplicate `knowledge_vectors` rows. The social path received the deterministic-UUID5 + `ON CONFLICT (signal_id) DO NOTHING` fix in Sprint 11 (Gap 2); global_news never did. **Day-run evidence (2026-07-23):** the duplicate-enrichment volume quantified under KG-A-7 lands here as duplicate vectors — making this a Domain-B retrieval-quality gap (near-identical vectors dilute HNSW recall), not only a storage-cost one. | Phase 7B.5-I (quantified 2026-07-23) | Medium | Deterministic UUID5 from content_hash (pattern exists in the social path — here the reference IS accurate) **✅ CLOSED going-forward-only 2026-07-27 (Phase 7D). Live: `knowledge_vectors` = `knowledge_vault` 1:1 in-window (newsapi 62=62, arxiv 102=102). Qualification: pre-existing `uuid4` rows are NOT retroactively deduped — a pre-existing article's first post-deploy re-delivery still writes one more vector (the new UUID5 does not collide with the old `uuid4` row); the whole-table debt (C0: 27,459 vectors vs 2,759 distinct articles = 9.95×) clears only by a DB reset, not by T5.** |
| KG-A-9 | Gold checkpoint fragility under dense backlog — synchronous ~1–2s enrichment stalls barriers → expiry → restart replay loops (2026-07-02 storm: 3,993 calls for 232 unique items) | Phase 7B.5-I | High | Checkpoint tuning / unaligned checkpoints / async enrichment |
| KG-A-10 | Local compose Flink leg broken — Beam Python-worker crash on first message (env gap since ~Sprint 17); in-process replay is the current local verification path | Phase 7B.5-I | Low | **✅ CLOSED 2026-08-01 (reproduction + fix).** **Trigger:** `infrastructure/.env` set `LOG_INFO_SAMPLE_RATE=1.0`, and `docker-compose.yml` hands both Flink legs the entire file via `env_file`. Setting it to `0.01` — single variable, nothing else — took the job from crash-on-first-message to stable: 0 OOMs, `Restarts=0`, 50 real messages processed end-to-end into `momentum_vault` (V1 acceptance passed in full). **The description above is wrong in a way that cost two months:** it is NOT a Beam Python-worker crash and NOT an env gap. The failure is `java.lang.OutOfMemoryError: Direct buffer memory` in the TaskManager JVM; the Beam noise ("Failed to start remote bundle", "CANCELLED: client cancelled", multiplexer hang-ups) is the gRPC channel torn down by the OOM. **The container survives** — measured `OOMKilled=false`, `Restarts=0`, 750 MiB of 7.6 GiB in use — and only the JOB dies, which is what sent investigation at container memory. **Raising a memory limit cannot fix this class:** direct buffer memory is bounded by Flink's off-heap config (`taskmanager.memory.*` → `-XX:MaxDirectMemorySize`), not by cgroups, so the JVM refuses the allocation before the kernel is involved. Compose `mem_limit` is thereby **ruled out by measurement**, not by argument. **Same defect as the Phase 7D cloud TaskManager OOM** — same variable, same first-message signature, same fix. **Scope of fix:** `.env` is gitignored so the working fix is local-only; `.env.example` now ships `0.01` with the failure mode documented, and both Flink services in `docker-compose.yml` pin `LOG_INFO_SAMPLE_RATE: "0.01"` in `environment:` so no `.env` value can reach the JVM. **MECHANISM NOT ESTABLISHED — see KG-A-17, which stays open.** Causation is proven; the path from the flag to buffer exhaustion is not. **Operationally important and independent of this defect: our Python records reach NO TaskManager sink at ANY sample rate** — measured 0 records in `docker logs`, `/opt/flink/log/flink--taskexecutor-*.log` and the UDF boot log at both `0.01` and `1.0`. Anyone debugging a Flink UDF in this project should know the application logs they are looking for do not arrive at all, whatever the sample rate. |
| KG-A-11 | Phase-7C scraper retirement removed `newspaper4k` from the manifests but did not fully clean the environment. `newspaper4k==0.9.5` lingered as an orphan in the local dev venv (absent from `requirements.txt`/`.lock`, imported by no code), and several of its transitive deps remain in `requirements.lock` as possible co-orphans (`lxml_html_clean`, `requests-file`, `tldextract`, `w3lib` — note `lxml`/`beautifulsoup4` are shared and are NOT co-orphans). The venv orphan was uninstalled during Sprint 26 (Domain B, 26.4, `pip uninstall newspaper4k`); the lock-side co-orphans were deliberately left untouched then to avoid dependency-surface movement before the baseline day-run. | Sprint 26 (surfaced by the 26.4 dependency add) | Low | A scoped Phase-7C venv/lock hygiene pass, post baseline day-run — audit each candidate co-orphan for remaining importers and remove only the truly unused. Do NOT `pip freeze` the dev venv before that audit (it would rewrite the whole lock). |
| KG-A-12 | Social path (hackernews) runs the keyword sniper — `process_hackernews_gold_message` carries the same `is_high_signal` guard as the global_news paths — but has **no semantic rescue and no `filter_rejects` capture**; both live only in `GlobalNewsGoldFunction`. A rejected story is skipped with a debug log and leaves no row, no cosine, no trace. Consequences: (a) HackerNews reject rate and filter quality are **unmeasurable**; (b) `funnel.csv` `rejects=0` for hackernews means "not captured", NOT "none rejected" — do not read it as a clean signal; (c) Phase 7B.5 calibration cannot cover this source. | Phase 7B.5-I day-run analysis (2026-07-23) | Medium | Extend semantic rescue + reject capture to the social path, or consciously accept HackerNews as un-calibrated and record that decision in the 7B.5 deliverable **✅ CLOSED 2026-07-27 (Phase 7D). 288 HN `filter_rejects` captured (50 distinct URLs), 0 NULL instance keys, `rescue_cosine` populated — in a source that captured nothing before. HackerNews is now calibratable by 7B.5 for the first time.** |
| KG-A-13 | Cost-instrumentation trace_id inconsistency: `compute_semantic_rescue()` passes `silver_doc.get("canonical_event_id", "")` directly, while every other `record_usage()` call site routes through the `_cost_trace_id()` helper with its `bronze_ref` fallback. A doc missing `canonical_event_id` therefore produces a `rescue_embed` cost row with an empty trace_id, silently breaking the S4.6 wasted-spend join (which matches cost rows against vault `canonical_event_id`). **Do not infer that the field was populated from the day-run's exact rescue accounting** (S4.5(b): 2,177 rescue_embed calls = 2,164 rejects + 13 rescued): that check compares row *counts* and never touches `trace_id`, so it is consistent with empty trace_ids and proves nothing about them. The population of `trace_id` is unmeasured to date and must be measured before any join-based figure — including the reported S4.6 wasted-spend number — is treated as reliable. | Phase 7B.5-I day-run analysis (2026-07-23) | Low | Route the rescue call site through `_cost_trace_id()`. Before relying on any S4.6-style join, first count empty-trace_id rows in `llm_cost_events` for the run window **✅ CLOSED 2026-07-27 (Phase 7D). Rescue call site routed through `_cost_trace_id()`; live C8 shows 0 empty `trace_id` across 1,717 in-window calls at every expected site.** |

| KG-A-14 | **Stranded-article gap.** An article already in `knowledge_vault` whose enrichment *then* failed (→ DLQ, no vector) is, on re-delivery, skipped by the T3 dedup gate and never receives a `knowledge_vectors` row — searchable full-text present, semantic vector absent. Sibling (opposite cause, same "never gets a vector" family, **pre-existing, NOT a D3 regression**): a record with a **malformed `document_hash`** makes `archive()` raise on every delivery, so T3 treats it as `archive_raised` (fail-open) and enriches it every time while it is never archived. | Phase 7D (T12) | Low | DLQ reprocessing, or disable the gate (`ENRICHMENT_DEDUP_GATE_ENABLED=false`). Follows from D3 as approved — record, not fix. |
| KG-A-15 | **`Dockerfile.flink` installs from `requirements.txt`, not `requirements.lock`** — every image rebuild re-resolves range-pinned deps from PyPI (the `-7d` build drifted from `-7b5i`: 22 transitive bumps, load-bearing set identical; `reports/phase7d-verify-20260727/image_freeze_diff.txt`). Recurs on every rebuild, incl. before a cost baseline. Adjacent to but distinct from KG-A-11 (that is orphans *inside* the lock; this is the image *bypassing* the lock). CLAUDE.md §4.4 says "Docker builds install from the lock file" — Dockerfile.flink does not. **Measured again 2026-08-01** (`1.19.1-pmcov`, built 5 days after `-7d`, compared by digest against `sha256:9a73a780…ebced303`): **0 of 118 packages moved.** Recorded as a measurement, not as reassurance — the exposure is **real but intermittent**, and an intermittent risk that has not yet fired is exactly the kind that gets quietly downgraded. One clean roll is not a fix; the next rebuild is an independent draw. | Phase 7D (2026-07-27); re-measured 2026-08-01 | Low | Point `Dockerfile.flink` LAYER 6 at `requirements.lock`, or accept + document the re-resolution. Until then, diff `pip freeze` against the deployed digest on **every** Flink rebuild — it is a two-minute gate and it is the only thing standing between a ranged bump and a silent behaviour change. |
| KG-A-16 | **HackerNews first-delivery lockout (follows from D1a, not a defect to fix).** Social archival precedes the `is_high_signal` check, so a story low-signal on its FIRST delivery is archived, and any LATER high-signal delivery is skipped by T4 (`already_archived`) and **never enriched** (the old comment-inclusive hash prevented this — fresh hash per delivery). D1a may therefore mean "enriched only if high-signal on first delivery" — a coverage reduction. **Not yet observed:** the 2026-07-27 slice showed disjoint sets (the 2 enriched HN stories had 0 overlap with the 288 rejects). | Phase 7D (2026-07-27) | Low | Record; revisit with a refresh-window key (D1 option c) if the multi-day run shows HN evidence load-bearing. |
| KG-A-17 | **OOM mechanism unresolved — STILL OPEN.** In the 7D cloud window `LOG_INFO_SAMPLE_RATE=1.0` was the differentiating variable across three TaskManager crash attempts at constant memory+burst (2560Mi and 6Gi both OOMKilled with it; 2560Mi survived without it), flat JVM heap placing growth Python-side. But the "~100× records over the Beam logging channel" story does NOT hold if INFO is unsampled either way (empirically it is, on the Flink UDF path — see KG-A-18). **The day-run (2560Mi, 9 sources, 24h) was Prometheus-clean → NOT KG-A-9.** **PROGRESS 2026-08-01 (KG-A-10 reproduction):** (a) the failure is now **reproducible locally on demand** — one variable, `1.0` crashes and `0.01` runs clean — so this no longer needs a cloud window to investigate; (b) the JVM error is specifically `OutOfMemoryError: **Direct buffer memory**` (off-heap NIO/Netty), not heap and not cgroup pressure — the container is never OOM-killed and sits at 750 MiB of 7.6 GiB; (c) **the duplicate-handler candidate above is ELIMINATED** — `utils/logging_config.py:setup_logging()` installs exactly one `StreamHandler` with `_TraceIdFilter` + `_SampledInfoFilter`, unconditionally, and never branches on `INFO_SAMPLE_RATE`; the handler set is identical at 1.0 and 0.01, so nothing about handler installation or propagation differs. The only thing the value changes is the return of `_SampledInfoFilter.filter()`. **MEASURED 2026-08-01 (local, single-variable, same 25 real messages):** records emitted by the TaskManager's Python worker are **0 at `0.01` and 0 at `1.0` — identical** — yet `1.0` OOMs on the first message and `0.01` runs clean. **Volume is therefore ELIMINATED as the mechanism on the failing path.** Our Python records reach **no TaskManager sink at all, at any sample rate**: `docker logs`, `/opt/flink/log/flink--taskexecutor-*.log` and the UDF boot log each contain zero `processing.silver_job` records at both settings (the only Python output present is Beam's own `INFO:root:` harness bootstrap). No log4j rule explicitly drops Beam/Python loggers either, so "dropped by log level" is not the explanation. **What remains (UNVERIFIED HYPOTHESIS):** the only thing the value changes is the return of `_SampledInfoFilter.filter()`; records that pass are handed onward and vanish before any sink we can find. The natural suspect is that they are still **transported** — Beam's Python harness forwards Python logging to the JVM over the FnLogging gRPC channel, which uses direct buffers — and discarded at the JVM end. That would explain the OOM and the zero visible records simultaneously. It has NOT been measured. | Phase 7D (2026-07-27); progressed + measured 2026-08-01 | Medium | **Defined next step, SCHEDULED POST-WIPE:** instrument `_SampledInfoFilter.filter()` return counts *inside the image* and compare at `1.0` vs `0.01`. Counting sinks cannot resolve this — it has already been done and returns 0/0. Deferred because it requires a `Dockerfile.flink` change, i.e. rebuilding the one component needed working for the wipe + collection run; the compose pin (`a951416`) makes the gap safe to carry meanwhile. Reproduces locally in ~5 min (compose from `infrastructure/`, `--build`); no cloud window needed. |
| KG-A-18 | **Documentation defect (record only — do NOT fix here).** `bringup_profiles.md` §5 trap 3 and `cluster_operations_guide.md` §11 state INFO is 1%-sampled for "anything using `setup_logging()`, which includes the agent and the Flink jobs." The **agent half is evidenced.** **⚠ RE-SCOPED 2026-08-01 — READ THIS BEFORE CITING THE FLINK HALF.** The original wording said the Flink half was "contradicted" by the 7D run (all 14 startup lines + full `[gold/dedup]` counts appearing with `LOG_INFO_SAMPLE_RATE` absent). **Those records were measured on SUBMIT-TIME CLIENT output — the `flink run` process — NOT on the TaskManager UDF path.** The two paths behave completely differently, and this entry says **nothing** about the UDF path. Measured 2026-08-01 on the UDF path itself: the Python worker's records reach **no TaskManager sink at all, at any sample rate** (0 records at both `0.01` and `1.0`; see KG-A-17). **Consequence, and the reason this re-scope could not wait:** for two months this entry was read as evidence that INFO is unsampled on the Flink path, which made the "log volume" mechanism for KG-A-10/KG-A-17 look refuted when it had never been tested on the path that actually fails. Anyone reasoning from the old wording reaches the opposite conclusion from the evidence. | Phase 7D (2026-07-27); re-scoped 2026-08-01 | Low | Correct both docs once KG-A-17's mechanism is understood. When doing so, state the client-vs-UDF path distinction explicitly — it is the part that misled. (The bring-up guide is being updated separately — do not double-edit.) |
| KG-A-20 | **Inactive Polymarket markets are placeholder legs, not markets — CLOSED at source 2026-08-01, agent-side guard still OPEN (KG-A-21).** **Mechanism:** Polymarket writes `outcomePrices` of `["0.5","0.5"]` to **template slots with no entity bound**, because there is nothing to price. Their questions are literally unnamed: "Will Company A be the largest company in the world", "Company B", "Company C" — slots in a candidate field awaiting a name. **The tell is the DISTRIBUTION, not the value:** measured live across the whole ≥$500k target set, **108 inactive markets on exactly ONE distinct price (0.5000) against 1,171 active markets on 305 distinct prices.** A single value shared by an entire status class is a default; a real price series is dispersed. **Record the tell, because it is how the next instance of this class gets caught** — the value 0.5 is unremarkable on its own and would pass any range guard, including ours ([0,1] inclusive). Once stored, that 0.5 is indistinguishable from a measurement, and it reached `momentum_vault` in the 2026-08-01 V5 window (108 of 1,282 rows, 8.4%). **Independently corroborated from the other side:** `server/src/repositories/trending.repository.ts` already filters `active !== false`, measured as removing 521 of 1,039 non-closed markets without dropping a single pickable market. **`archived` is NOT the same case** and must not be swept up — its markets carry real distinct prices (0.49, 0.255, 0.25 on the three observed) and real questions. | 2026-08-01 (V5 cloud window) | Medium | **✅ Producer-side FIXED 2026-08-01** (not yet deployed — ships with the next image build): `_market_skip_reason` drops `status == "inactive"` specifically, never "not active", surfaced as a third `inactive` counter in the discovery funnel. Status derivation extracted to `_derive_market_status()` so the filter and the emitted payload cannot drift. Verified live: collectable 1,282 → 1,173, inactive 108 → 0, archived 3 → 3, rows at exactly 0.5 → **0**. |
| KG-A-21 | **No agent-side defence against a non-active market row (deferred follow-up to KG-A-20).** **Nothing in the agent path branches on `status` at all.** `market_bridge._pack_polymarket_payload` carries `market_status` onto the payload and **no consumer reads it**; A4's resolved-market guard keys on `end_date_iso`; `_apply_resolved_market_guard` flips `has_ended` only on a live `closed` status or a past end date. An `inactive` row therefore resolves normally and renders an invented "Market Consensus 50%" beside a real forecast. KG-A-20's producer fix stops new such rows entering, but **there is no defence at the point of use** — against rows already stored, against a producer regression, or against any future source of a non-active row. | 2026-08-01 (raised with KG-A-20) | Medium | The agent should refuse to resolve against a row whose `status` is not `active`, falling through to A5's existing explicit refusal (`NO_MATCH_CAPTION`) rather than fabricating a benchmark. Deliberately deferred, not rejected: it needs an agent image rebuild, which the next window needs anyway for V4. |
| KG-A-22 | **`conditionId` never reaches the agent — A1's deterministic path is built and DORMANT.** Verified live 2026-08-01 across three cloud forecasts: `conditionId` is `null` on every `sessions/` and `forecastQueries/` document, and every resolution logged `via=question-match`. **Traced hop by hop; exactly one break, at [`client/src/pages/DashboardPage.tsx:239`](client/src/pages/DashboardPage.tsx#L239): `await onCreateSession(question, idempotencyKey)` — the third argument is dropped.** `CreateForecastView` passes three (`onSubmit(question, randomUUID(), conditionId)`) from BOTH the picker path (:295-297) and the binary inline path (:157); `handleSubmitForecast` forwards two. **This is the single common point** — which is why both a picker submission and a binary submission were null, and why "binary events have no picker" does not explain it. `DashboardPage` WAS edited by the partner's `61cedb5` and its prop TYPE was updated (:69-74, three-arg with doc comment) but the call site was not; TypeScript cannot catch it because `conditionId?` is optional, so a two-arg call type-checks. Everything either side is correct and verified: picker → call site → `CreateSessionInput` → POST body → route zod schema → handler (`parsed.data` whole) → service → `session.repository.ts:443` → `claim_session.py:162`. **Corroboration that only this hop is broken:** the `conditionId` key is PRESENT with value `null` on all four documents inspected, so the deployed BFF is running current code and writing the field — it simply never receives a value. **Consequence:** resolution quality today rests entirely on the picker pasting the market question verbatim. That is copying, not matching, and the first non-verbatim question breaks it. Our receiving end (A1) is complete and needs no change — it activates the moment a non-null id arrives. | 2026-08-01 (V6 cloud window) | Medium | **Partner's fix, in `client/` — one line.** `DashboardPage.tsx:239` → `await onCreateSession(question, idempotencyKey, conditionId)`, plus threading `conditionId` through `handleSubmitForecast`'s signature. Re-verify by confirming a `sessions/` doc carries a non-null `conditionId` and the resolution logs `via=conditionId`. |
| KG-A-23 | **The raise-on-100%-failure contract was never generalised — 5 of 7 DAG producers report SUCCESS while emitting 0 Bronze.** Phase 9.5 Stage B Item 4 established that a producer must raise when 0-of-N units succeed, so Airflow marks the task `failed` and monitoring has one clean signal (`cluster_operations_guide.md` §5.2 states this as the contract). **It is implemented in exactly two files** — [`opensky_producer.py:612`](data-pipeline/ingestion/opensky_producer.py#L612) and [`googletrends_producer.py:450`](data-pipeline/ingestion/googletrends_producer.py#L450), both guarded as `if run_emitted == 0 and len(...) > 0: raise RuntimeError`. Those are precisely the two producers that were already known-broken (KG-C-7, pytrends 404), so the guard was added reactively where failures were *observed* and never applied to the producers that were working. **`fred`, `arxiv`, `newsapi`, `hackernews` and `openweather` have no zero-emit guard at all.** Caught live 2026-08-15: FRED's 9-of-9 series returned HTTP 400 (malformed key, KG-C-14), the producer caught each per-series error and skipped, logged `emitted 0 messages total`, returned `None`, and Airflow recorded `state=success`, exit code 0. On the `0 7 * * *` schedule that reads green indefinitely while collecting nothing. **The severity is that a green DAG is not evidence of collection for 5 of 7 producers** — any conclusion drawn from "all DAGs succeeded" is unsound without checking Bronze offsets independently. | 2026-08-15 cloud bring-up (FRED 400s) | **High** | Lift the `run_emitted == 0` guard out of the two producers into a shared helper in `utils/` (CLAUDE.md §3.2 — never duplicate it seven times) and call it from every producer's run summary. Until then, verify each producer by Bronze offset delta, not by DAG state. Pairs with KG-C-14 — the parser bug produced the failure, this gap is why it was invisible. **What protects the 2026-08-15→Thursday continuous run is offset-delta verification, NOT DAG state:** every one of the 7 producers was proven that day by a measured Bronze offset increase plus an ERROR-line count on the task log, and the vault row it produced. **Anyone reading these DAGs green later must not infer collection from that** — 5 of the 7 will report success while emitting nothing. Re-verify by offset before trusting any "all green" reading. |
| KG-A-19 | **Low-signal duplicate rescue-embedding waste (record, deferred to 7B.5).** The T3 gate sits *after* the semantic-rescue block, so a low-signal duplicate pays a rescue embedding on every re-delivery (the gate never sees it — it returns before `kv_archive`). Day-run: ~910 repeat rescue evals/day (~42% of rescue calls, ~10% of window API calls); on the HN path T6 capture precedes T4, so a persistent front-page low-signal story is re-captured every pulse. Cost is noise (embeddings 0.8%) but each counts against RPD. | Phase 7D (Ron 2026-07-26) | Low | 7B.5's call — one reject row per DISTINCT article vs per instance may be better for calibration. Bears on the §8.5 PVC headroom check. |

> **7B.5 hand-off (from the 7D cloud window, not a Domain-A gap):** HN max `rescue_cosine` observed = **0.3523,
> above the 0.35 promote threshold** — the first counter-example to D2's premise (day-run max ~0.345 sat below).
> n=1 of 288, not a reversal, but exactly D2's revisit condition. Per D4, HN cosines are NOT comparable to the
> news-calibrated 0.35 (the sniper reference vector is news-built), so 7B.5 must set a *social* threshold on
> social data. Also: arxiv high-signal papers duplicate ~9×/run vs low-signal ~1.05× — cross-listing may itself
> be a relevance signal (`reports/phase7d-verify-20260727/close_pack_summary.md`).

> **Proposal for Ron's decision (Domain C, KG-C-10) — NOT implemented:** declare `replicas: 0` as the resting
> state in the four pipeline manifests (`flink-jobmanager`, `flink-taskmanager`, `telegram`, `polymarket`) and
> always bring them up via `kubectl scale` — the pattern `agent-worker` already uses. That would close KG-C-10
> structurally instead of re-creating the drift on every profile switch. Recorded here for a decision.

> KG-A-3, KG-A-4, and KG-A-5 are pipeline-producer concerns that were first **surfaced
> during Phase 9.5 (cluster robustness)**; they are owned here as Domain A ingestion gaps,
> not by the cloud domain.

"""
agent/utils/ — shared utilities used by hub agents and nodes.

Single-source-of-truth modules for cross-cutting computations (recency
weighting, etc.) so the same logic is not duplicated across agents.
"""


/CLAUDE.md
/data_contracts_and_sources.md
/deployment_and_sprints.md
/pipeline_core.md
/project_overview.md
/storage_and_agent.md
/folder-structure.txt
/newsapi_end_to_end.md
/.claude
/firebase_test.py
/firestore-debug.log

# Local environment files — never commit. Use the .env.example files as templates.
.env
.env.local
**/.env
**/.env.local
!**/.env.example




.DS_Store

# Local dev tooling state — per-machine (SQLite DB, agent/job scratch dirs).
# Both rules are needed: a leading slash anchors the pattern to the repo root, so
# `/dev/` matches ONLY a top-level dev/ and silently leaves `data-pipeline/dev/`
# tracked — which is where the Polymarket API-shape scratch work actually lands.
/dev/
data-pipeline/dev/

# Generated pipeline report — regenerated per run, never reviewed as source.
# Ignored rather than relying on "remember not to stage it": a safeguard that
# depends on remembering fails the first time anyone runs `git add -A`.
/data-pipeline/PIPELINE_REPORT.md
